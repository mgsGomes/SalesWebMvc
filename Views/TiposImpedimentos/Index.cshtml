@model IEnumerable<SalesWebMvc.Models.TipoImpedimento>


@{
    ViewData["Title"] = "Tipos de Impedimentos";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-default">Novo Impedimento</a>
</p>

<table class="table table-striped table-hover">
    <thead>
        <tr class="success">
            <th>
                @Html.DisplayNameFor(model => model.Impedimento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Impedimento)
                </td>                
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Editar</a>                    
                    <a asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>


